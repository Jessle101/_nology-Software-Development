nmb = 78;

// Checks if number is even
if (nmb % 2 === 0) {
  // If even log in console
  console.log("The number " + nmb + " is EVEN");
} else {
  // If odd log outcome console
  console.log("The number" + nmb + "is ODD");
}
